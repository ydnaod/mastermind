import './OpenButton.css';
import React, { useState, useEffect, Fragment } from 'react';

function OpenButton() {
  return (
    <Fragment>
      <button>Open</button>
    </Fragment>
  );
}

export default OpenButton;